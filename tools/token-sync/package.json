{
  "name": "@eni/token-sync",
  "version": "0.0.0",
  "private": true,
  "description": "Figma ↔ @eni/tokens sync tool",
  "scripts": {
    "sync": "ts-node src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ./src"
  },
  "devDependencies": {
    "@eni/config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
