{
  "name": "@eni/auth",
  "version": "0.0.0",
  "description": "Authentication hooks and providers — EniTrance (Firebase Auth) integration",
  "exports": {
    ".": "./src/index.ts",
    "./hooks": "./src/hooks/index.ts",
    "./providers": "./src/providers/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ./src"
  },
  "dependencies": {
    "@eni/schemas": "workspace:*"
  },
  "devDependencies": {
    "@eni/config": "workspace:*",
    "@types/react": "^19.0.0",
    "firebase": "^11.0.0",
    "react": "^19.0.0",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "firebase": "^11.0.0",
    "react": "^19.0.0"
  }
}
